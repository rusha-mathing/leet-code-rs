/// The structure definition for this module can be found in list.rs
pub mod list;
use list::ListNode;
pub struct Solution;
type Node = Option<Box<ListNode>>;

impl Solution {
    pub fn remove_nth_from_end(head: Node, n: i32) -> Node {
        todo!()
    }
}
